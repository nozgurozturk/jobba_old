
const {
  PORT,
  MONGO_USERNAME,
  MONGO_PASSWORD,
  MONGO_HOSTNAME,
  MONGO_PORT,
  MONGO_DB,
  JWT_LIFE_TIME,
  JWT_SECRET
} = process.env

module.exports = {
  PORT,
  MONGO_USERNAME,
  MONGO_PASSWORD,
  MONGO_HOSTNAME,
  MONGO_PORT,
  MONGO_DB,
  JWT_LIFE_TIME,
  JWT_SECRET
} 
